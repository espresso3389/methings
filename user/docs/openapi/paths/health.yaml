health:
  get:
    operationId: getHealth
    summary: Server health check
    tags: [Health]
    responses:
      '200':
        description: Server is running.
        content:
          application/json:
            schema:
              type: object
              required: [status, service, termux]
              properties:
                status:
                  type: string
                  const: ok
                service:
                  type: string
                  const: local
                termux:
                  type: string
                  enum: [ok, offline, starting, stopping]
                  description: Status of the Termux worker.
            example:
              status: ok
              service: local
              termux: ok
